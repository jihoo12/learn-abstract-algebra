 집합 A에서 집합 B로의 **함수(function)** 또는 **사상(mapping)** $\phi$ 는 집합 A의 각 원소 a에 집합 B의 원소 b를 정확히 하나씩 대응하는 규칙이다.
 * 이때 집합 A를 함수 $\phi$ 의 **정의역(domain)**이라 하고, B를 함수 $\phi$ 의 **공역(codomain)**이라고 한다.
 * 함수 $\phi$ 에 의해 a가 b에 대응하면 b를 a에 대한 **상(image)**이라고 한다.
 * 집합 A의 원소들의 모든 상으로 이루어진 집합 B의 부분집합을 함수 $\phi$ 의 **치역(range)**이라고 한다.
